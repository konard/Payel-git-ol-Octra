{
  "name": "@octra/desktop",
  "private": true,
  "version": "0.2.0",
  "description": "Octra Desktop — an Electron shell that runs the real Octra web app as its renderer, adding a frameless title bar, native menu, recent-projects store and a filesystem-backed file explorer.",
  "main": "dist-electron/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "dev": "OCTRA_DEV=1 npm run start",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:filesystem": "node scripts/check-filesystem.mjs",
    "test:projects": "node scripts/check-projects.mjs",
    "test:menu-model": "node scripts/check-menu-model.mjs",
    "test:static-server": "node scripts/check-static-server.mjs",
    "test:main": "node scripts/check-electron-main.mjs",
    "test": "npm run build && npm run test:filesystem && npm run test:projects && npm run test:menu-model && npm run test:static-server && npm run test:main"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "^33.0.0",
    "typescript": "^5.5.4"
  }
}
