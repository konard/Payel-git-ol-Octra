name: API Gateway CI

on:
  push:
    branches: [master, main]
    paths:
      - 'apigateway/**'
      - '.github/workflows/apigateway-ci.yml'
  pull_request:
    paths:
      - 'apigateway/**'
      - '.github/workflows/apigateway-ci.yml'

concurrency:
  group: apigateway-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build & test (apigateway)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apigateway
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.4'
          cache-dependency-path: apigateway/go.sum

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./... -v -count=1
