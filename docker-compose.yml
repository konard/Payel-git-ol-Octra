services:
  postgres:
    image: postgres:15-alpine
    container_name: octra-postgres
    environment:
      POSTGRES_USER: octra
      POSTGRES_PASSWORD: octra_password
      POSTGRES_DB: octra
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - octra-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U octra"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: octra-redis
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - octra-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # backend — the Octra monolith. Replaces the former user / agents /
  # orchestrator / apigateway microservices with a single Go service that
  # aggregates AI CLIs and exposes a personal MCP endpoint per user.
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: octra-backend
    env_file:
      - .env
    environment:
      - HTTP_ADDR=:8080
      - DB_DNS=host=postgres user=octra password=octra_password dbname=octra port=5432 sslmode=disable
      - REDIS_URL=redis://redis:6379/0
      - ENVIRONMENTS_DIR=/var/lib/octra/environments
      - CLI_TTL=30m
    ports:
      - "8080:8080"
    volumes:
      - octra_environments:/var/lib/octra/environments
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - octra-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend/web
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=
        - VITE_FRONTEND_MODE
    container_name: octra-frontend
    env_file:
      - .env
      - .env.prod
    environment:
      - FRONTEND_PORT=80
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_started
    networks:
      - octra-network
    restart: unless-stopped

  # poster:
  #   build:
  #     context: ./poster
  #     dockerfile: Dockerfile
  #   container_name: octra-poster
  #   env_file:
  #     - .env
  #   environment:
  #     - TG_BOT_TOKEN_POSTER=${TG_BOT_TOKEN_POSTER}
  #     - CHANNEL_ID=@octra_ai
  #     - POSTGRES_HOST=postgres
  #     - POSTGRES_PORT=5432
  #     - POSTGRES_USER=octra
  #     - POSTGRES_PASSWORD=octra_password
  #     - POSTGRES_DB=octra
  #     - BASE_ULR=${BASE_ULR:-https://api.gonkagate.com/v1/chat/completions}
  #     - API_KEY=${API_KEY}
  #     - AI_MODEL=${AI_MODEL:-moonshotai/kimi-k2.6}
  #     - REPO_ROOT=/repo
  #   volumes:
  #     - .:/repo:ro
  #   working_dir: /repo
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - octra-network
  #   restart: unless-stopped

networks:
  octra-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  octra_environments:
