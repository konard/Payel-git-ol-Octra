Добавить в настройки вкладку интеграций с другими сервисами:
lefine.pro (там же activity pub тут прийдётся помучаться) нужна activity pub интеграция на фронтенде @frontend/web/src/hooks/useLefinePro.ts с https://exchange.lefine.pro он должен  подписываться на него и забирать задачу из /outbox и переправлять её уже по привычный сокетам по ворк флоу который пользователь ранее создавал пользователь сам выбирает ворк флоу и уже по нему отправляется задача на выполнение потом когда zip возвращается мы его сразу ложим в /inbox 

Но перед этим нужно добавить в настройки вкладку "интеграции" соответственно её нужно будет перевести на все доступные языки

Там интеграции примерно будут так выглядить:

 ---------------------------------
|    -------------------          |
|   |   |               |         |
|    ------------------           |
|    -------------------          |
|   |   |               |         |
|    ------------------           |
|                                 |
|                                 |
|                                 |
|                                 |
|                                 |
 ---------------------------------

Я очеь старался показать короче с лева иконка интеграции будет для lefine.pro: frontend\web\src\images\lefine.pro.jpg
для тг: frontend\web\src\images\Telegram.webp

Пользователь нажимает кнопку "Интегрировать" и там появляется на этом же экране форма которую можно будет свернуть там кастомизиция можно поставить галочку "использовать ключ по умолчанию" тогда он будет братья уже из пользователя (в настройках есть прикол что можно ключ по умолчанию поставить) либо просто ввести его вручную конкретно для этой интеграции потом выбираешь для неё ворк флоу и сохраняешь

Что касаемое телеграмма бот уже сделан просто надо будет сделать так что бы когда ты нажимаешь интегрировать он тебе отправляет ссылку на бота и там ты уже авторизуешся и на фронтенде показывает что ты интегрировал телеграмм